import React from "react";
import { Tabs } from 'antd';
import Header from '../component/header';
const { TabPane } = Tabs;
function Home(props) {
  return (
    <div>
      <Header />
    </div>
  );
}

export default Home;
